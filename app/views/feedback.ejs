<!DOCTYPE html>
<html>
<head>
	<!-- Use the 'title' property to render a page title  -->
	<title><%= title %></title>
    <link rel="stylesheet" type="text/css" href="css/bootstrap.css" />

</head>
<body>
	<!-- Load the static image file  -->
	
	<form action="/users/<%= user.id%>?_method=PUT" method="post">
        <div class="outer_container">
            <!-- Use the 'title' property to render a title element  -->
            <h2 class="jumbotron text-center">  <%= title %></h2>
            <% if(user.role=='Admin'){%>
            <div class="container">
                <span><b><a href="/display">Display Customer Feedback</a></b></span> 
                &nbsp;
                
            </div>
            <% } %>
            <div class="container">
               <span><b><a href="http://localhost:3000">Logout</a></b></span>
            </div>
            <div class="container">
                <input type="hidden" class="fields" name="id" value="<%=user.id%>" readonly />
            </div>
            <div class="container">
                <label class="control-label">First Name:</label>
                <input type="text" class="form-control" name="firstName" value="<%=user.firstName%>" readonly />
                <span class="help-block"></span>
            </div>
            <div class="container">
                <label class="control-label">LastName:</label>
                <input type="text" class="form-control" name="lastName" value="<%=user.lastName%>" readonly />
                <span class="help-block"></span>
            </div>
            <div class="container">
                <label class="control-label">Address:</label>
                <input type="text" class="form-control" name="address" value="<%=user.address%>" readonly />
                <span class="help-block"></span>
            </div>
            <div class="container">
                <label class="control-label">Technical Skill:</label>
                <input type="text" class="form-control" name="skill" value="<%=user.skill%>" readonly />
                <span class="help-block"></span>
            </div>
            <div class="container">
                <label class="control-label">Email:</label>
                <input type="text" class="form-control" name="email" value="<%=user.email%>" readonly />
                <span class="help-block"></span>
            </div>

            <div class="container">
                <label class="control-label">Role:</label>
                <input type="text" class="form-control" name="role" value="<%=user.role%>" readonly />
                <span class="help-block"></span>
            </div>
            <div class="container">
                <label class="control-label">Comment:</label>
                <textarea size="400" class="form-control" name="comment" placeholder="<%=user.comment%>"><%=user.comment%></textarea>
                <span class="help-block"></span>
            </div>
            &nbsp;
            <div class="container">
                <button type="submit" class="btn btn-success btn-block">Submit Feedback</button>
            </div>

        </div>
</form>
</body>
</html>